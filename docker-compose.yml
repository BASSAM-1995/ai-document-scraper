version: "3.9"

services:
  api:
    build: .
    container_name: ai-doc-scraper
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./output:/app/output
    restart: unless-stopped
